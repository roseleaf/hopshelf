.flip
  .midpage
    %img(src="#{@genre.path}" class="genpic")
    .box
      %h2.santa Books in #{@genre.name}:
    
    .blip
      - @books.each do |book|
        %tr
          %td= link_to book.title.capitalize, book
          %br/
          %br/
      -if @books.empty?
        
        %p Nothing in this subject yet...

    %br
    .nlnk
      =link_to "Subjects", "/genres/"
